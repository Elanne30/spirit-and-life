"use client";
import { useActionState, useState } from "react";
import { createArticleDraftAction } from "@/app/admin/(protected)/actions/article-content";
import { ReflectionBodyEditor } from "@/app/admin/(protected)/content/reflection-body-editor";
const initialState = { status: "idle" as const, message: "" };
function slugify(value: string) { return value.normalize("NFKD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 120); }
export function ArticleDraftForm() { const [state, action, pending] = useActionState(createArticleDraftAction, initialState); const [title, setTitle] = useState(""); const [slug, setSlug] = useState(""); const [edited, setEdited] = useState(false); return <form className="admin-form admin-draft-form" action={action} encType="multipart/form-data"><input type="hidden" name="contentType" value="article" /><h3>Article Information</h3><label htmlFor="article-title">Title</label><input id="article-title" name="title" value={title} onChange={(e) => { setTitle(e.target.value); if (!edited) setSlug(slugify(e.target.value)); }} placeholder="Article title" required /><label htmlFor="article-slug">Slug</label><input id="article-slug" name="slug" value={slug} onChange={(e) => { setEdited(true); setSlug(e.target.value.toLowerCase().replace(/[^a-z0-9-]+/g, "-").replace(/-+/g, "-").replace(/^-+|-+$/g, "").slice(0, 120)); }} required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" maxLength={120} /><label htmlFor="article-date">Date</label><input id="article-date" name="date" placeholder="August 18, 2026" /><label htmlFor="article-reading-time">Reading time</label><input id="article-reading-time" name="readingTime" placeholder="8 min read" /><label htmlFor="article-category">Category</label><select id="article-category" name="category" defaultValue="Philosophy"><option>Philosophy</option><option>Apologetics</option><option>Theology</option><option>Christian Living</option><option>Faith & Life</option><option>Biblical Studies</option><option>Church History</option></select><label htmlFor="article-tags">Tags</label><input id="article-tags" name="tags" placeholder="Philosophy, Apologetics, Faith" /><label htmlFor="article-introduction">Introduction</label><textarea id="article-introduction" name="introduction" rows={6} placeholder="Set up the question or argument..." /><label className="admin-checkbox" htmlFor="article-featured"><input id="article-featured" name="featured" type="checkbox" value="yes" />Feature this article</label><div className="admin-structure-column"><h3>Article Body</h3><ReflectionBodyEditor /></div><div className="admin-draft-actions"><button className="button button-secondary" name="saveMode" value="draft" disabled={pending}>{pending ? "Saving..." : "Save draft"}</button><button className="button button-primary" name="saveMode" value="continue" disabled={pending}>{pending ? "Saving..." : "Save & continue editing"}</button></div>{state.message ? <p className={state.status === "error" ? "form-error" : "form-note"} role="status">{state.message}</p> : null}</form>; }
