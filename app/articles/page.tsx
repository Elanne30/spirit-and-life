import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { listPublishedArticles } from "@/app/content/repository";
export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Articles", description: "Developed writing on philosophy, apologetics, difficult questions, culture, faith, and ideas worth thinking through." };
export default async function ArticlesPage() { const articles = await listPublishedArticles(); return <main className="page-container content-index-page"><header className="content-index-header"><p className="eyebrow">Articles</p><h1>Ideas worth thinking through.</h1><p>Developed writing on philosophy, apologetics, difficult questions, culture, faith, and the arguments that shape what we believe.</p></header>{articles.length ? <div className="content-index-grid">{articles.map((article) => <article className="content-index-card" key={article.contentSlug}><Link className="content-index-card-image" href={`/articles/${article.contentSlug}`}><Image src={article.image} alt={article.title} width={1280} height={853} sizes="(max-width: 720px) 100vw, 50vw" /></Link><div className="content-index-card-body"><div className="home-card-meta"><span>{article.category}</span><span>{article.readingTime}</span></div><h2><Link href={`/articles/${article.contentSlug}`}>{article.title}</Link></h2><p>{article.introduction}</p><Link className="content-card-link" href={`/articles/${article.contentSlug}`}>Read article <span aria-hidden="true">→</span></Link></div></article>)}</div> : <p className="empty-state">Articles will appear here when they are published.</p>}</main>; }
